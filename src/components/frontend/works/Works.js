import React from 'react';
import WorksList from './util/works/WorksList';
import Types from './util/Types';

export default () => {
    return (
        <div>
            <Types />
            <WorksList />
        </div>
    );
};