import React from 'react';

import SubContainer from '../../util/SubContainer';

export default () => {
    return (
        <SubContainer paddingTop paddingBottom borderBottom>
            types
        </SubContainer>
    );
};